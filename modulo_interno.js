module.exports = {
    soma(a,b){
        let somado = a + b
        console.log("Final: " + somado)
        return somado.toString()
    }
}

module.exports = {
    subtração(a,b){
        let sub = a - b
        console.log("Final: " + sub)
        return sub.toString()
    }
}

module.exports = {
    multiplicação(a,b){
        let multi = a * b
        console.log("Final: " + multi)
        return multi.toString()
    }
}

module.exports = {
    Divisão(a,b){
        let dividido = a / b
        console.log("Final: " + dividido)
        return dividido.toString()
    }
}

module.exports = {
    Porcentagem(a,b){
        let porc = a % b
        console.log("Final: " + porc)
        return porc.toString()
    }
}